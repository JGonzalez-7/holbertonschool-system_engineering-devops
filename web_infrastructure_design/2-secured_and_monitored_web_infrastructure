```mermaid
graph TD
    User["User's Computer / Browser"] -->|HTTPS request to www.foobar.com| DNS["DNS Resolver"]

    DNS -->|Resolves to Load Balancer IP| FW1["Firewall 1"]

    FW1 --> LB["Load Balancer: HAProxy"]

    subgraph LBBox["Load Balancer Server"]
        LB
        SSL["SSL Certificate for www.foobar.com"]
        MonitorLB["Monitoring Client"]
    end

    LB -->|SSL termination / forwards traffic| FW2["Firewall 2"]
    LB -->|SSL termination / forwards traffic| FW3["Firewall 3"]

    FW2 --> Server1["Server 1"]
    FW3 --> Server2["Server 2"]

    subgraph Server1Box["Server 1"]
        Nginx1["Web Server: Nginx"]
        App1["Application Server"]
        Code1["Application Files / Codebase"]
        DB1["MySQL Primary Database"]
        Monitor1["Monitoring Client"]

        Nginx1 -->|Forwards requests| App1
        App1 -->|Uses| Code1
        App1 -->|Reads/Writes| DB1
    end

    subgraph Server2Box["Server 2"]
        Nginx2["Web Server: Nginx"]
        App2["Application Server"]
        Code2["Application Files / Codebase"]
        DB2["MySQL Replica Database"]
        Monitor2["Monitoring Client"]

        Nginx2 -->|Forwards requests| App2
        App2 -->|Uses| Code2
        App2 -->|Reads| DB2
    end

    Server1 --> Server1Box
    Server2 --> Server2Box

    DB1 -->|Primary-Replica replication| DB2

    MonitorLB -->|Collects metrics and logs| Monitoring["Monitoring Service: Sumologic / Datadog / New Relic"]
    Monitor1 -->|Collects metrics and logs| Monitoring
    Monitor2 -->|Collects metrics and logs| Monitoring
```
